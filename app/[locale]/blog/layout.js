export const metadata = {
    title: "Our Blogs | Naans & Curries",
    description: "Stay updated with the latest insights and culinary stories from Naans & Curries.",
};

export default function BlogLayout({ children }) {
    return <>{children}</>;
}
